<h1>Burgers!</h1>

<h2>Burgers that are not devoured!</h2>

<ul>
  {{#each burgers}}
  {{#unless sleepy}}
  {{> burgers/cat-block sleep=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are sleepy!</h2>

<ul>
  {{#each burgers}}
  {{#if sleepy}}
  {{> burgers/cat-block sleep=false}}
  {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form id="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Sleepy?</label><br>
    <input type="radio" name="devoured" value="1" id="devoured" checked> Sleepy!<br>
    <input type="radio" name="devoured" value="0" id="awake"> Awake
  </div>

  <button type="submit">Add Cat</button>
</form>